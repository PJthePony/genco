// Product defaults. The "calendar assistant" is the agent CC'd on
// emails that suggest a meeting. Defaults to the Tanzillo suite's own
// Luca instance; users can override in Settings.
export const DEFAULT_CALENDAR_ASSISTANT_EMAIL = "luca@tanzillo.ai";
